---
title: Overview
hide_table_of_contents: true
---

# SAML Jackson

Jackson implements the SAML login flow as an OAuth 2.0 flow, abstracting away all the complexities of the SAML protocol. Integrate SAML with just a few lines of code.

import Link from '@docusaurus/Link';
import ThemedImage from '@theme/ThemedImage';
import ArrowIcon from '../../static/img/arrow.svg';
const frameworks = [
  {
    name: 'Next.js',
    href: '/guides/jackson/with-nextjs',
  },
  {
    name: 'Remix.run',
    href: '/guides/jackson/with-remix',
  },
  {
    name: 'Express.js',
    href: '/guides/jackson/with-express',
  },
  {
    name: 'RedwoodJS',
    href: '/guides/jackson/with-redwood',
  },
  {
    name: 'SuperTokens',
    href: '/guides/jackson/with-supertokens',
  },
  {
    name: 'Next-Auth',
    href: '/guides/jackson/with-nextauth',
  },
];
const deployments = [
  {
    name: 'Heroku',
    href: '/guides/jackson/with-heroku',
  },
  {
    name: 'Vercel',
    href: '/guides/jackson/with-vercel',
  },
  {
    name: 'AWS',
    href: '/guides/jackson/with-aws',
  },
];

## Integrations

SAML Jackson works with a large number of tools and frameworks.

<div class="container" style={{ padding: 0 }}>
  <div class="row is-multiline">
    {frameworks.map((framework) => (
      <div key={framework.name} class="col col--3">
        <Link class="card" to={framework.href}>
          <div
            class="card__body"
            style={{ display: 'flex', gap: 20, alignItems: 'center' }}
          >
            <ArrowIcon width="30" style={{ display: 'block', maxHeight: 30 }} />
            <p>{framework.name}</p>
          </div>
        </Link>
      </div>
    ))}
  </div>
</div>

## Deployment

If you want to self host SAML Jackson, the recommended approach is to use one of the recommended one click installation options.

<div class="container" style={{ padding: 0 }}>
  <div class="row is-multiline">
    {deployments.map((deployment) => (
      <div key={deployment.name} class="col col--3">
        <Link class="card" to={deployment.href}>
          <div
            class="card__body"
            style={{ display: 'flex', gap: 20, alignItems: 'center' }}
          >
            <ArrowIcon width="30" style={{ display: 'block', maxHeight: 30 }} />
            <p>{deployment.name}</p>
          </div>
        </Link>
      </div>
    ))}
  </div>
</div>
